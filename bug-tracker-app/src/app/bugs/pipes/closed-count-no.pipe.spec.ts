import { ClosedCountNoPipe } from './closed-count-no.pipe';

describe('ClosedCountNoPipe', () => {
  it('create an instance', () => {
    const pipe = new ClosedCountNoPipe();
    expect(pipe).toBeTruthy();
  });
});
